# Veraset API Key
# Get yours at: https://www.veraset.com/contact
VERASET_API_KEY=your_api_key_here

# AWS Credentials for S3
# IMPORTANT: Never commit real credentials. Use environment variables in your deployment platform.
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=us-west-2

# S3 Bucket for config and data storage
S3_BUCKET=your-s3-bucket-name

# API Base URL (for server-side calls)
NEXT_PUBLIC_API_URL=https://gmc-mobility-api.vercel.app

# Optional: Mapbox token for map visualizations
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token_here

# Optional: GMC Auth (if you want to protect endpoints)
GMC_API_SECRET=your_secret_here

# Authentication (for production deployment)
# IMPORTANT: Use strong, unique credentials in production
# Generate secure values:
#   AUTH_SECRET: openssl rand -base64 32
#   AUTH_PASSWORD: Use a strong password manager-generated password
AUTH_USERNAME=your_username_here
AUTH_PASSWORD=your_strong_password_here
AUTH_SECRET=your-random-32-char-secret-here

# Optional: Password hashing (for enhanced security)
# If set, AUTH_PASSWORD will be hashed using these values
# Generate hash: node -e "const crypto=require('crypto'); const salt=crypto.randomBytes(16).toString('hex'); const hash=crypto.createHash('sha256').update('your_password'+salt).digest('hex'); console.log('AUTH_PASSWORD_HASH='+hash); console.log('AUTH_PASSWORD_SALT='+salt);"
# AUTH_PASSWORD_HASH=your_password_hash_here
# AUTH_PASSWORD_SALT=your_password_salt_here

# CORS Configuration (for production)
# Comma-separated list of allowed origins, e.g., "https://example.com,https://app.example.com"
# Leave empty to disable CORS (most secure)
ALLOWED_ORIGINS=https://your-app-domain.vercel.app
